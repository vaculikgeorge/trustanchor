public/
resources/
.hugo_build.lock

PRE-PUBLISH-CHECKLIST.md

# Local-only orphan pages (CV, project references) — never published
private/

# Local diagram tooling (Excalidraw generators + Playwright node_modules) — never published
tools/
